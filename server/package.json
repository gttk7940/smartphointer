{
  "name": "smartphointer-signaling",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "deploy": "gcloud run deploy smartphointer-signaling --project smartphointer --source . --region asia-northeast1 --allow-unauthenticated --min-instances 0 --max-instances 1 --memory 128Mi --cpu 1"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.20.5",
    "typescript": "~5.9.3"
  }
}
